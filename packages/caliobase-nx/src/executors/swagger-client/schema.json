{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "SwaggerClient executor",
  "description": "",
  "type": "object",
  "properties": {
    "input": {
      "type": "string",
      "description": "path to an openapi/swagger spec file"
    },
    "output": {
      "type": "string",
      "description": "path to the directory where the generated client will be saved"
    },
    "generateSpecTarget": {
      "type": "string",
      "description": "a target to be executed that should generate a swagger file"
    },
    "dotenv": {
      "type": "string",
      "description": "path to a dotenv file to load vars from"
    }
  },
  "required": []
}
